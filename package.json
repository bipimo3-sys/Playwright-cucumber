{
  "name": "playwright-cucumber",
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright + Cucumber + Chai test setup",
  "main": "index.js",
  "scripts": {
    "gitpush": "git add . && git commit -m \"committed\" && git push origin main",
    "start-server": "node server.js",
    "test": "cucumber-js --import steps/**/*.js --import support/**/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^11.0.0",
    "chai": "^5.1.0",
    "playwright": "^1.56.0",
    "dotenv": "^17.2.3",
    "@types/node": "^24.7.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
